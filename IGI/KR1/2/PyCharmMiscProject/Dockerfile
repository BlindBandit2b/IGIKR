# Используем официальный образ Python
FROM python:3.9-slim

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем скрипт и файл в контейнер
COPY app.py /app/app.py

# Запускаем скрипт
CMD ["python", "app.py"]
]